"""Configuration for Preview Generator Agent."""

AGENT_CONFIG = {
    "name": "Preview Generator",
    "allowed_tools": [],  # This agent only generates text, no tools needed
    "max_turns": 3,
    "permission_mode": "default"
}