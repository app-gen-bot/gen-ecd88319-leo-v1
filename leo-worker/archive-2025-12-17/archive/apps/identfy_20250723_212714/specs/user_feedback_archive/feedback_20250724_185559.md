In the /cases for each case, seems like we want to show images, but currently no images are being shown. Can we use the Dalle tool to generate the stock photos that are photo realistic and use them in the app for the cases?
In settings/security, we are getting a 404. Fix this.
Similarly for settings/privacy, we are getting a 404. Fix this.
Similary for settings/notifications, we are getting a 404. Fix this.
The conversion funnel under benchmarking seems weird. Fix this