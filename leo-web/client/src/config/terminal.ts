/**
 * Terminal display configuration constants
 */
export const TERMINAL_CONFIG = {
  // Truncation limits (characters)
  TOOL_INPUT_TRUNCATE: 5000,
  THINKING_BLOCK_TRUNCATE: 500,  // existing value for thinking blocks
} as const;
